apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monitoring-ingress
spec:
  rules:
  - host: "my-grafana.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service: 
            name: grafana
            port:
              number: 80
  - host: "my-nginx.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: my-nginx
            port:
              number: 80
  - host: "my-prometheus.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: prometheus
            port:
              number: 80
